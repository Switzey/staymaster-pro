import { ModulePlaceholder } from "@/components/dashboard/module-placeholder";
import { modulePages } from "@/lib/modules";

export default function BookingsPage() {
  return <ModulePlaceholder module={modulePages.bookings} />;
}
